{
  "name": "aws-lambda-openai-integration",
  "version": "1.0.0",
  "description": "AWS Lambda function triggered by EventBridge that fetches data from REST API and integrates with OpenAI",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "zip -r function.zip . -x \"*.git*\" \"node_modules/.bin/*\" && aws lambda update-function-code --function-name lambda-openai-integration --zip-file fileb://function.zip"
  },
  "keywords": [
    "aws",
    "lambda",
    "openai",
    "eventbridge"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.0",
    "dotenv": "^16.3.1",
    "openai": "^4.20.0"
  }
}
